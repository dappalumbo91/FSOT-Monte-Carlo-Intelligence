# Optional export snapshots

Shipped JSON snapshots from a local scientific run (regenerate anytime):

```powershell
python -m fsot_mc scientific-audit
python -m fsot_mc flip-protocols
python -m fsot_mc claims
python -m fsot_mc readings --n-paths 64
```

These are **illustrative** of structure, not a substitute for re-running on your machine.
