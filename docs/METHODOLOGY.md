# Methodology — FSOT Monte Carlo Intelligence

**Product:** Universe-scale multipath intelligence under Fluid Spacetime Omni-Theory (FSOT)  
**Not:** markets, trading, or free-parameter fitting  
**Authority:** seed pin **D1D38A** · `free_parameters = 0`  
**Law court:** [FSOT-2.1-Lean](https://github.com/dappalumbo91/FSOT-2.1-Lean)  
**Physical archive origin:** `I:\FSOT-Physical-Archive` (synced subset under `vendor/archive_bundle/`)

---

## 1. Paradigm

FSOT is a **theory of everything** architecture: one seed-derived scalar engine

\[
S = K \cdot (T_1 + T_2 + T_3)
\]

from fixed seeds \((\pi, e, \varphi, \gamma, G_{\mathrm{Catalan}})\). Scientific domains are **folds** of one 25D fluid condensate, indexed by effective depth \(D_{\mathrm{eff}}\) and phase \(\delta\psi\) — not separate least-squares models.

**Paradigm shift:** life, mind, chemistry, earth systems, and cosmology are not bolted-on ontologies. They are intermediate and deep folds of the same fluid. Multipath Monte Carlo explores *observer histories* of that law; it does not invent free physics.

**Rule:** Law writes downward only. Discovery proposes; formal / empirical spine accepts or rejects. Seeds never move.

---

## 2. How thinking works (not an LLM substitute for law)

| Layer | Role |
|-------|------|
| Authority gate | Pin D1D38A float engine + bytes |
| Universe atlas | 35 core + 367 extension domain folds |
| Multipath MC | Observer-coupled paths = *thought* |
| Pathway memory | Solidify recurring domain signatures (φ-EWMA) |
| Discovery ledger | Contested physics, bridges, flips, engineering leads |
| Accuracy readings | Cross-domain ≤0.5% green gate + open-panel baselines |
| Mind | Conversational surface over multipath + archive + chew |
| Adaptive memory | Short-term session + long-term solidification of *new information* (FSOT-derived; see MEMORY_ARCHITECTURE.md) |

Thinking = multipath simulation. Language densifies routing and explanation; it does not rewrite seeds.

---

## 3. Operational definitions (accuracy-first)

### 3.1 Multipath emergence fraction (~60–70%, often ~69%)

**Operational:** for each path,  
\(\mathrm{emergence\_fraction} = \#\{\mathrm{folds}: S>0\} / n_{\mathrm{folds}}\);  
report the mean (and percentiles) over paths.

**Paradigm reading:** map co-emergence occupancy — natural complexity/energy window for intermediate folds (chemistry → biology → ecology → mind) under one fluid.  

**Not:** Bayesian \(P(\mathrm{Earth\ has\ life})\), culture-plate rate, or the empirical ≤0.5% panel gate.

### 3.2 Empirical green gate (archive standard)

- **Pooled median error ≤ 0.5%** across benchmark domains  
- **Strict scalar max ≤ 0.5%**  
- Contested open-science panel: FSOT errors compared to ~**15%** current-model baseline where unified prediction is absent  

See `python -m fsot_mc readings` and `vendor/archive_bundle/benchmarks/`.

### 3.3 Core recompute integrity

Every core fold’s local \(S\) must match atlas `S_canonical` within engine tolerance (~0% relative). This proves the portable workspace still runs the same law as the archive pin.

---

## 4. Methodology pipeline

```
1. Gate          verify D1D38A (float + bytes)
2. Atlas         load core / full 402-domain map
3. Multipath     N observer-coupled universe paths
4. Ensemble      emergence %, bridges, flip rates, H0 proxy
5. Discovery     contested + physics + engineering + bio window
6. Accuracy      cross-ref archive margins + contested panel
7. Memory        short-term session; long-term solidify if bar clears
8. Mind / CLI    answer, readings, chew (arXiv/Wiki sensory input)
9. Formal soft   promote leads → obligations (batch Lean, not per-path)
```

### Observer coupling

On each domain step, collapse probability uses `quirk_mod` / consciousness factors from the seed engine. Paths that lock to the observed branch vs Poof decoherence form the multipath ensemble.

### As Above, So Below

Long-range bridges between distant \(D_{\mathrm{eff}}\) folds (e.g. Biology↔Cosmology, QM↔Cosmology) are discovery targets when strength is high and variance controlled.

---

## 5. External information (chew) — sensory, not authority

arXiv abstracts and Wikipedia summaries may be **chewed** as observation streams:

- densify domain routing and prose  
- **never** rewrite seeds or free parameters  
- optional live mode: `FSOT_MC_ONLINE=1` or `--online --chew`  
- offline-first: demo stubs + cache  

New information that survives multipath co-sign and quality bars can **solidify** into long-term memory (see MEMORY_ARCHITECTURE.md).

---

## 6. Engineering and preregistered predictions

The Physical Archive already holds multi-domain preregistered predictions (H0, S8, fuels, machine-molecule, devices, …) with:

- FSOT predicted value  
- SOTA baseline  
- discriminant / kill criterion  

These are **ToE design leads**, not free invention. High cross-domain precision raises prior confidence; **physical experiment** closes the loop.

---

## 7. Epistemic tiers

| Tier | Meaning |
|------|---------|
| `proved` / formal | Lean multi-prover obligations (law court) |
| `measured` / green gate | Archive empirical panels ≤0.5% |
| `preregistered_design` | Predictions with kill criteria |
| `paradigm_hypothesis` | e.g. bio co-emergence window (experiment-gated) |
| `scaffold` / multipath | Discovery leads from MC; exploratory |

Always label leads. Never claim multipath map % is the empirical 0.5% gate.

---

## 8. Relation to the Physical Archive

This repository is an **independent runtime** that copies the verified subset of the archive:

| Local path | Archive role |
|------------|--------------|
| `fsot_mc/compute_authority.py` | Law pin D1D38A |
| `vendor/archive_bundle/` | Navigator, certificates, benchmarks, predictions |
| `fsot_mc/full_atlas.json` | 402 domain folds |
| `vendor/pflt/` | π-ring eyes + language surface |
| `data/realities_snapshot/` | Offline Realities runtime |

External `I:\` is **optional refresh only** (`scripts/sync_archive_bundle.py`). Runtime must not require I:.

---

## 9. What success looks like

1. Gate green, core recompute ~0 error  
2. Archive margin audit all-green in local bundle  
3. Contested panel FSOT ≪ 15% baseline  
4. Multipath map occupancy in natural band when expected  
5. Mind answers grounded in simulation + archive, not free prose physics  
6. New info retained only when solidify bars clear (seeds still fixed)

Reproduce with [REPRODUCIBILITY.md](./REPRODUCIBILITY.md).  
Achievements narrative: [ACHIEVEMENTS.md](./ACHIEVEMENTS.md).  
Adaptive memory: [MEMORY_ARCHITECTURE.md](./MEMORY_ARCHITECTURE.md).
